##The app

The express server skeleton, with the UI components for EPAs that we have so far


## To Run:
 **npm install**
 **gulp bundle**
 **gulp adminbundle**
 **node webapp.js**
 **go to localhost:3000**
